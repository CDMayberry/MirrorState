using System;
using Mirror;
using UnityEngine;

namespace MirrorState.Scripts.Networking
{
    // A class to shortcut actions to MirrorStatePhysics and other utilities for gameobjects.
    public class NetworkedStateBehaviour : NetworkBehaviour
    {
        /*protected void Raycast(uint tick, Vector3 origin, Vector3 direction, Action<int, RaycastHit[]> actionOnHit)
        {
            MirrorStatePhysics.Raycast(tick, hasAuthority, origin, direction, actionOnHit);
        }*/
    }
}
